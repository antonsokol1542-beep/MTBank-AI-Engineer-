# Environment — contains real secrets, never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Whisper / ML model weights (downloaded at runtime into cache, never committed)
# NOTE: do NOT ignore a bare `models/` dir — that is the app's Pydantic package (api/models).
*.bin
*.pt
*.onnx
.cache/

# Test audio (test_data/*.wav) IS committed on purpose — required by the assignment.

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Docker volumes
openwebui_data/
pipelines_data/
